export const HOST = "127.0.0.1";
export const HTTP_BASE_URL = `://${HOST}`;
export const HTTPS_BASE_URL = `https://${HOST}`;
export const PATH = "/run";
export const RESPONSE_BODY = JSON.stringify({ message: "Hello world" });
